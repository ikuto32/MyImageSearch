# Optional local configuration. Copy to .env and set your own paths.
# Load explicitly with: uv run --env-file .env python app.py --port 5000
MYIMAGESEARCH_IMAGE_DIR=./images
MYIMAGESEARCH_META_DIR=./clip_meta
